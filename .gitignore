# Supabase SQL migrations — run manually in the SQL Editor, never deployed to
# GitHub Pages (and never public).
sql-editor/

# Local reference material (screenshots etc.), not part of the live site.
Example images/

# Internal project docs — kept local, not published to the public repo.
CLAUDE.md
docs/
